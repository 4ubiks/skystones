// initializes stuff
struct Player initializePlayer1(struct Player, int teamNumber);
struct Player initializePlayer2(struct Player, int teamNumber);
struct Board initializeBoard(struct Board board);
int pieceStatus(struct Board board, int pieceNumber);